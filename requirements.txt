anthropic>=0.40.0
litellm>=1.50.0
langgraph>=0.2.50
langchain-core>=0.3.20
httpx>=0.27.0
pydantic>=2.9.0
python-dotenv>=1.0.0
rich>=13.7.0
pandas>=2.0
# UI scaffolding (optional — only needed for ui/app.py)
streamlit>=1.35
mygene>=3.2
watchdog>=4.0
openpyxl>=3.1        # read .xlsx reagent-inventory uploads (Experiment → Reagent inventory)
boltz-api>=0.1            # Boltz binding scores (needs your own paid API key)
reportlab>=4.0           # PDF report inside the run export zip
